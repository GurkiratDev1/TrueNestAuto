# .env.example
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/truenest
PORT=3000
NODE_ENV=development

# Optional service keys for workers (do not commit real keys)
SERVICE_ROLE_KEY=changeme
SUPABASE_URL=http://localhost:5432
SUPABASE_ANON_KEY=changeme
